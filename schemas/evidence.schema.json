{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://brainstorming.local/schemas/evidence.schema.json",
  "title": "BrainStorming Evidence Contract",
  "type": "object",
  "required": ["topic_slug", "findings"],
  "additionalProperties": true,
  "properties": {
    "schema_version": {
      "type": "string",
      "const": "1",
      "description": "Schema version for migration detection. Must be \"1\"."
    },
    "topic_slug": {
      "type": "string",
      "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$"
    },
    "generated_on": {
      "type": "string",
      "pattern": "^\\d{4}-\\d{2}-\\d{2}$"
    },
    "created": {
      "type": "string",
      "pattern": "^\\d{4}-\\d{2}-\\d{2}$"
    },
    "findings": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/finding"
      }
    },
    "metrics": {
      "type": "object",
      "additionalProperties": true
    }
  },
  "$defs": {
    "finding": {
      "type": "object",
      "required": ["id", "confidence"],
      "additionalProperties": true,
      "properties": {
        "id": { "type": "string" },
        "title": { "type": "string" },
        "claim": { "type": "string" },
        "summary": { "type": "string" },
        "confidence": {
          "type": "string",
          "enum": ["HIGH", "MEDIUM", "LOW", "UNVERIFIED"]
        },
        "generated_by_phase": {
          "type": "string",
          "description": "Phase or agent that produced this finding (e.g. phase_1_researcher)."
        },
        "must_survive": { "type": "boolean" },
        "source_count": { "type": "number" },
        "source_ids": {
          "type": "array",
          "items": { "type": "string" }
        }
      }
    }
  }
}
